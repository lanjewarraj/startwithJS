//Math Objects
let val;

val = Math.max(1,4,2,1,-1);
val = Math.pow(2,10);
val = Math.round(2.44);
val = Math.sqrt(81);
val = Math.min(-1,0,-2,3);
val = Math.abs(-30);
val = Math.PI;
val = Math.E;
//random number generator between 0 and 1 but u can generate random number as u want by multiplying the number u want 
val = Math.random();

val = Math.round(Math.random()*10 +1);
console.log(val);